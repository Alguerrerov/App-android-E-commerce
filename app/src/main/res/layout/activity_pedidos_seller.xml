<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_card">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Pedidos"
            android:textColor="@color/text_primary"
            android:textSize="22sp"
            android:textStyle="bold"/>

        <TextView
            android:id="@+id/btnNotificaciones"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🔔"
            android:textSize="22sp"/>
    </LinearLayout>

    <!-- Buscador -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginHorizontal="16dp"
        android:background="@color/bg_section"
        android:paddingHorizontal="12dp"
        android:paddingVertical="8dp"
        android:gravity="center_vertical">

        <EditText
            android:id="@+id/etSearch"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Buscar pedidos..."
            android:textColor="@color/text_primary"
            android:textColorHint="@color/state_inactive"
            android:background="@null"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="⚡"
            android:textSize="18sp"/>
    </LinearLayout>

    <!-- Tabs -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:layout_marginTop="12dp"
        android:paddingHorizontal="16dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tabTodos"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Todos (56)"
                android:textColor="@color/accent_blue"
                android:textSize="13sp"
                android:paddingBottom="6dp"
                android:layout_marginEnd="20dp"/>

            <TextView
                android:id="@+id/tabPendientes"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Pendientes (8)"
                android:textColor="@color/state_inactive"
                android:textSize="13sp"
                android:paddingBottom="6dp"
                android:layout_marginEnd="20dp"/>

            <TextView
                android:id="@+id/tabEnProceso"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="En proceso (16)"
                android:textColor="@color/state_inactive"
                android:textSize="13sp"
                android:paddingBottom="6dp"
                android:layout_marginEnd="20dp"/>

            <TextView
                android:id="@+id/tabCompletados"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Completados (32)"
                android:textColor="@color/state_inactive"
                android:textSize="13sp"
                android:paddingBottom="6dp"/>
        </LinearLayout>
    </HorizontalScrollView>

    <!-- Lista -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvPedidos"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="16dp"
        android:clipToPadding="false"/>

    <!-- Footer -->
    <TextView
        android:id="@+id/tvFooter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Mostrando 1-6 de 56 pedidos"
        android:textColor="@color/state_inactive"
        android:textSize="12sp"
        android:padding="16dp"/>

</LinearLayout>